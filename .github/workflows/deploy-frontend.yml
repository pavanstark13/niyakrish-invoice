# Disabled — Vercel auto-deploys from GitHub directly, no CLI needed
# Duplicate triggers were causing blocked deployments
name: Deploy Frontend (disabled)
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel deploys automatically from GitHub push to main"
